------------------
Antes de rodar o código:
- Fiz ele usando JavaScript, caso queira rodar o código é necessário instalar o "prompt-sync" pois com ele é possível pegar o input do usuário usando Node.js
- Para usar "prompt-sync" deve usar o comando "npm install prompt-sync" (sem as aspas) no terminal do VSCODE, depois disso o código vai rodar sem problemas

Sobre o exercício:
- Esse exercício foi praticamente o mesmo do anterior, só que com a diferença de ficar em loop eterno até o usuário escolher sair, como eu já tinha
feito esse loop no exercício passado, fiz apenas alguns ajustes em relação ao código do que foi pedido nesse exercício
- Exemplo do código rodando: https://i.ibb.co/WK3pLpr/calculadora2.png
------------------

const ps = require('prompt-sync');
const input = ps();

let calculadora = function (valor1, valor2, operacao) {
    if (operacao === 1) {
        return valor1 + valor2;
    }else if (operacao === 2) {
        return valor1 - valor2;
    }else if (operacao === 3) {
        return valor1 * valor2;
    }else if (operacao === 4) {
        const divisao = valor1 / valor2;
        return divisao.toFixed(2);
    }
}

let menu = function() {
    console.log('\nQual operação deseja fazer?');
    console.log('1 - Soma\n2 - Subtração\n3 - Múltiplicação\n4 - Divisão\n0 - Sair');
    console.log('-----------------------------------');
}

let menuFinal = function() {
    console.log('\nDeseja fazer uma nova operação?');
    console.log('1 - Sim\n2 - Não');
}

console.log('--- CALCULADORA VERSÃO 2 ---');
while (true) {
    menu();
    let operacao = parseInt(input('Operação: '));
    if (operacao >= 0 && operacao <= 5) {
        if (operacao != 0) {
            let valor1 = parseFloat(input('Informe o 1º valor: '));
            let valor2 = parseFloat(input('Informe o 2º valor: '));
            console.log('Resultado: ',calculadora(valor1, valor2, operacao));
            console.log('-----------------------------------');
            menuFinal();
            operacao = parseInt(input('Operação: '));
            if (operacao === 1) {
                continue;
            }else {
                console.log('Saindo da calculadora....');
                break;
            }
        }else {
            console.log('Saindo da calculadora....');
            break;
        }
    }else {
        console.log('\nEssa opção não existe!');
        continue
    }
}
    
